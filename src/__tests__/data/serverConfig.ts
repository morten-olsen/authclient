import ServerConfig from '../../configs/Server';

const serverResponse: ServerConfig = {
  authorization_endpoint: 'https://my-oauth.com/authorize',
  token_endpoint: 'https://my-oauth.com/token',
};

export default serverResponse;